import { SherpaClient } from "../index";
import { SourceMaps } from "../../shared/sourcemaps";
export type { SourceMaps };
export declare const enabled: (client: SherpaClient) => boolean;
export default function (client: SherpaClient, self: Self): void;
